import org.apache.tools.ant.taskdefs.condition.Os

plugins {
    id 'java-library'
}

group 'net.janrupf'

processResources {
    from(nativeBinaries) {
        into "native-binaries"
    }
}
