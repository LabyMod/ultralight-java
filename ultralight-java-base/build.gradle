plugins {
    id 'java-library'
    id 'maven-publish'
}

group 'net.janrupf'

processResources {
    from(nativeBinaries) {
        into "native-binaries"
    }
}

commonPublish(project) {
    pom {
        name = "UltralightJava"
        description = "Ultralight bindings for Java"
    }
}
